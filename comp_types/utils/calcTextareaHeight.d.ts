export default function calcTextareaHeight(uiTextNode: any, minRows?: any, maxRows?: any, useCache?: boolean): {
    height: string;
    minHeight: string;
    maxHeight: string;
    overflowY: string;
};
